# Netlify build configuration for the Quartz CTF writeups site.
# See https://docs.netlify.com/configure-builds/file-based-configuration/

[build]
  command = "npm ci && npx quartz build"
  publish = "public"

[build.environment]
  NODE_VERSION = "22"
