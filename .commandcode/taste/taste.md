- Prefers bash scripts for local deploy/sync automation (e.g., requested a `deploy.sh` to sync content to servers). Confidence: 0.8
- Uses GitHub Actions for CI/CD, auto-deploying on commit+push to the main branch. Confidence: 0.8
- Hosts infrastructure on Oracle Cloud (OCI) VMs with nginx + Let's Encrypt/certbot, using DuckDNS dynamic DNS for domains. Confidence: 0.7
- Prefers memorable, topic-relevant naming (e.g., a `writeups` subdomain) and domain aliases over duplicate configurations. Confidence: 0.6
- Develops on Windows with VSCode; bash scripts must be run under Git Bash — on this machine plain `bash` resolves to WSL (which can't see the Windows `node` on PATH), so `deploy.sh` is invoked via the explicit Git Bash path (`C:\Users\sadiq\AppData\Local\Programs\Git\bin\bash.exe deploy.sh`). Confidence: 0.8
- Values polished, modern web design with dynamic/animating elements (e.g., glassmorphism, particle networks, intro loaders) and likes matching a reference site's colors/styling. Confidence: 0.7
- Prefers brand/decorative text (e.g., the "SIGSEGV" wordmark) to live persistently in the layout — like the right sidebar — with a looping animation, rather than as a one-time transient full-screen intro overlay. Confidence: 0.6
- Open to changing frameworks/tooling when it better serves the goal; not wedded to the current stack (e.g., willing to drop Quartz). Confidence: 0.6
- Prefers "hacker"/Kali Linux terminal aesthetics for terminal-like UI (dark navy/black background, cyan/blue accents, bash-style prompts like `┌──(user㉿host)-[~]`); explicitly asks for the fake terminal to match a Kali reference image. Confidence: 0.8
- Wants decorative/fake UI (e.g., the site's terminal) to actually work like the real thing — a virtual shell with functional `ls`, `ls challenges/`, `cd`, `pwd`, etc., rather than a static visual ("i know its not a real terminal but itd be cool"). Confidence: 0.8
- Wants the site's terminal to open with useful content already on screen — default state lands inside `~/challenges` with the `ls` listing pre-rendered rather than an empty prompt (content visible at first glance, no interaction required). Confidence: 0.6
- When restyling or removing UI elements, expects the existing features/functionality to remain intact (e.g., "obviously have the other features the same"). Confidence: 0.6
- Prefers minimal, non-redundant navigation/UI — e.g., avoids duplicate site-title headings and removes unused UI toggles like the reading-mode button. Confidence: 0.7
- Prefers clean, non-overlapping UI layouts — e.g., finds a header that overlaps/cuts through the search bar annoying. Confidence: 0.6
- Values consistent visual theming across assets — recolors/restyles icons and images to match the site palette rather than leaving them as-is. Confidence: 0.6
- Communicates visual intent by attaching images (@image.png) — reports UI problems and specifies the desired look/theme via a reference screenshot, expecting the issue or design to be derived from the image. Confidence: 0.9
- The fake terminal's placement has oscillated over time (main page → right sidebar → main page → right sidebar); the latest request integrates it into the right sidebar on every page, styled as part of the sidebar rather than a standalone window. The SIGSEGV brand loop remains in the right sidebar site-wide as persistent global UI. Confidence: 0.5
- Wants team member handles/credits displayed as part of the site's persistent brand element (usernames listed below the brand wordmark), styled consistently with the brand text (same font/animation/loop) but in the theme's blue accent to distinguish them. Confidence: 0.6
- Prefers UI elements to be integrated into the surrounding layout rather than pasted in as standalone "window" boxes — e.g., the fake terminal was restyled to blend with the sidebar's brand typography (no window chrome, no dark box), and the user explicitly said "don't copy and paste it in... as a window, it will look weird." Confidence: 0.8
- Expects custom UI components to be theme-aware and adapt to both light and dark mode using the site's CSS variables rather than hardcoded colors (e.g., "make it change colours for light mode"). Confidence: 0.7
- Delegates design/styling decisions to the agent while specifying the goal/aesthetic direction ("you have full authority over the styling, change it however you want"). When a design attempt misses the mark, expects a complete from-scratch redo of the component (typography, spacing, sizing, theme colors) so it looks native to the page rather than an incremental patch ("do the entire terminal design from scratch... doesnt look like it was just pasted in from somewhere else"). Confidence: 0.8
- When rebranding, expects the old name/branding to be removed everywhere (config, UI components, README, generated output) and the new branding applied consistently site-wide. Confidence: 0.5
- Wants interactive UI (like the sidebar terminal input) to remain fully usable — no horizontal scrolling/overflow; content must fit the container width and inputs need enough room to type ("a user doesnt have to scroll to the side, and give space to type"). Confidence: 0.8
- Wants every site change deployed immediately — expects the agent to always run the deployment script (`deploy.sh`) after making any change, pushing the build to the OCI VM right away rather than waiting for CI or a manual deploy. Confidence: 0.9
